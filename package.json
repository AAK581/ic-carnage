{
  "name": "ic_carnage",
  "version": "1.0.0",
  "description": "Welcome to your new `IC Carnage` project and to the Internet Computer development community. By default, creating a new project adds this README and some template files to your project directory. You can edit these template files to customize your project and to include your own code to speed up the development cycle.",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build required' && exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@dfinity/agent": "^3.2.2",
    "@dfinity/auth-client": "^3.2.2",
    "@dfinity/identity": "^3.2.2"
  }
}
